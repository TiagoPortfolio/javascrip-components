import './index.scss';

const Header = total => `<header class="header alignCenter">
                    <div class="page-title">My Bag</div>
                    <div class="page-total">${total}</div>
                </header>`;

export default Header;
